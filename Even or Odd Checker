Build a Lexer and Parser that reads numbers and prints whether each is Even or Odd.

Required Tokens
NUMBER
NUMBER: integer.

Grammar Rules
program : program NUMBER | NUMBER

Sample Input
4 7 10

Expected Output
4 → Even 7 → Odd 10 → Even
👉 Focus: Simple condition inside parser · One token type.

# ─── LEXER ────────────────────────────────────────────
import ply.lex as lex
import ply.yacc as yacc

tokens = ['NUMBER']

def t_NUMBER(t):
    r'\d+'
    t.value = int(t.value)
    return t

t_ignore = ' \t'

def t_error(t):
    t.lexer.skip(1)

lexer = lex.lex()

# ─── PARSER ───────────────────────────────────────────
def p_program_multiple(p):
    '''program : program NUMBER'''
    n = p[2]
    label = "Even" if n % 2 == 0 else "Odd"
    print(f"{n} → {label}")

def p_program_single(p):
    '''program : NUMBER'''
    n = p[1]
    label = "Even" if n % 2 == 0 else "Odd"
    print(f"{n} → {label}")

def p_error(p):
    print(f"Syntax error at: {p}")

parser = yacc.yacc()

# ─── TEST ─────────────────────────────────────────────
data = "4\n7\n10"
parser.parse(data, lexer=lexer)
